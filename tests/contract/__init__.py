# Contract tests: verify the shapes this repo exposes to the outside
# world (the Agent Contract, and the Pydantic schemas that mirror ORM
# models) stay valid and in sync — independent of unit, integration,
# or model correctness, which the other three test categories cover.
# This is the fourth test category the EDD names alongside those three.
