# The nightly scoring job entrypoint (nightly_score_job.py) lives here.
# It composes the same service-layer functions the API uses rather
# than re-implementing scoring logic — added once that service logic
# exists.
