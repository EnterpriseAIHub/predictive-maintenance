# One-off operational scripts — not imported by the API or services.
# seed_demo_data.py populates a handful of demo assets so a fresh
# deployment has something to demonstrate rather than an empty
# database. Idempotent: safe to run more than once.
