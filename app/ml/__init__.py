# Feature engineering (features.py), the offline training pipeline
# (training/), and per-prediction SHAP explainability (explain.py) are
# implemented. The inference wrapper (Phase 5) — load-once model
# loading combined with prediction + explanation — comes next.