# Feature engineering (features.py), the offline training pipeline
# (training/), per-prediction SHAP explainability (explain.py), and
# the load-once inference wrapper (inference.py) are all implemented.
# The service layer (Phase 6) is the next consumer of this package.
 