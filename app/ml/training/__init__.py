# Offline training pipeline: dataset loading and model training/
# evaluation/artifact-saving. Nothing here runs at request time — it
# is invoked manually or via a scheduled retrain job, never imported
# by the API or service layer.
